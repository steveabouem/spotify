

const initialState = { user: {}, search: { album: "", artist: ""}, results: []};

export default function ( state = initialState, action) {
  switch(action.type) {
    default:
    return state;
  }
}